
【目标】：执行观测记忆提取并直接持久化到磁盘。
【基准日期】：2026-06-27

【幂等性约束】：在执行任何写入前，**必须先**读取 OBSERVATIONS.md，检查是否已存在 `Date: 2026-06-27` 的条目。若存在，则**不要进行任何文件修改**，直接回复「Entry for 2026-06-27 already exists, skipping」即可。

【SOP 路径】：
/Users/unicorn/context-infrastructure/periodic_jobs/ai_heartbeat/docs/KNOWLEDGE_BASE.md

【任务内容】：
1. **获取 Context**：请阅读上述 SOP 以及其中引用的 L3 约束文件。
2. **幂等性检查**：读取 OBSERVATIONS.md，若已有 `Date: 2026-06-27` 则跳过后续步骤。
3. **扫描与过滤**：自主扫描根目录（/Users/unicorn/context-infrastructure）下的变动。
4. **写入记忆**：将针对 2026-06-27 的 🔴 🟡 🟢 观测结果直接写入或追加到 `/Users/unicorn/context-infrastructure/contexts/memory/OBSERVATIONS.md`。**鼓励使用命令行 append**（如 `echo "..." >> OBSERVATIONS.md` 或 `tee -a`），避免对大文件做全文编辑。
5. **范围约束**：**仅执行 L1 Observer 任务**。不要执行 SOP 中提到的 L2 Reflector 任务（即不要修改 `rules/` 下的任何文件，不要进行规则晋升或垃圾回收）。
6. **格式规范**：
   - 日期 Header 必须严格使用 `Date: YYYY-MM-DD` 格式（Date 首字母大写，冒号后空格，日期为 ISO 格式）。
   - 在结果文件中提到任何文件或目录时，**必须使用相对于根目录的完整路径**（例如：`rules/skills/workflow_deep_research_survey.md`），不要只写文件名。
7. **汇报**：完成后，在此回复一个简短的 Walkthrough。
